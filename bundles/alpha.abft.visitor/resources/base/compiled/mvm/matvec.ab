affine matvec { N | -1 + N >= 0  }
	input
		float A { i,j |  i >= 0  &&  -1 + N - i >= 0  &&  j >= 0  &&  -1 + N - j >= 0  };
		float x { i |  i >= 0  &&  -1 + N - i >= 0  };
	output
		float b { i |  i >= 0  &&  -1 + N - i >= 0  &&  -1 + N >= 0  };
	let
		b = reduce(+, (i,k->i), ((i,k->i,k)@(A) * (i,k->k)@(x)));
.
