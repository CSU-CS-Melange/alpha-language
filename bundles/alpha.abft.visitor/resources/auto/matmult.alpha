
affine matmult_aabft [N] -> {  : N > 0 }
	inputs
		A : {[i, j]: 0 <= i < N and 0 <= j < N }
		B : {[i, j]: 0 <= i < N and 0 <= j < N }
		A_2 : {[i, j]: 0 <= i < N and 0 <= j < N }
		B_2 : {[i, j]: 0 <= i < N and 0 <= j < N }
	outputs
		C : {[i, j]: 0 <= i < N and 0 <= j < N }
		C_2 : {[i, j]: 0 <= i < N and 0 <= j < N }
	when {  : N > 0 } let
		C[i,j] = reduce(+, (i,j,k->i,j), (A[i,k] * B[k,j]));
.
